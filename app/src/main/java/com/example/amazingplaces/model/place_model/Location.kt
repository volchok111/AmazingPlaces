package com.example.amazingplaces.model.place_model

/**
 * A Location is a pair of latitude and longitude values.
 * @property {Double} latitude - The latitude of the location.
 * @property {Double} longitude - The longitude of the location.
 */
data class Location(
    val latitude: Double,
    val longitude: Double
)